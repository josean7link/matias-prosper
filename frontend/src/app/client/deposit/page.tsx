import { PageHeader, ComingSoon } from "@/components/Page";
export default function ClientPage() {
  return (<div>
    <PageHeader kicker="Client" title="Cargar / Retirar" />
    <ComingSoon what="Cargar / Retirar" />
  </div>);
}
